name: ci

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  api:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: bowling
          POSTGRES_PASSWORD: bowling
          POSTGRES_DB: bowling
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U bowling -d bowling"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - name: Test
        env:
          DATABASE_URL: postgres://bowling:bowling@localhost:5432/bowling?sslmode=disable
        run: go test ./...

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: web/package-lock.json
      - name: Install
        run: npm install
      - name: Test
        run: npm test

